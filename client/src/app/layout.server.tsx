import type { Metadata } from "next";
import RootLayout from "./layout";

export const metadata: Metadata = {
  title: "KinWell Pharmacy",
  description: "KinWell Pharmacy Portal",
};

export default RootLayout;
